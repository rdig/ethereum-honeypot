/* @flow */

export type StatusCodesType = {
  SUCCESS: number,
};

/*
 * Maybe find a better name for this?
 */
export type MimeTypesType = {
  JSON: string,
};

export type ServerArgumentsType = {
  port?: number,
};
